% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MIRDD.R
\name{MIdiagRDD}
\alias{MIdiagRDD}
\title{Diagnostic Tool by Multiple Imputation for Regression Discontinuity Designs}
\usage{
MIdiagRDD(
  y,
  x,
  cut,
  seed = 1,
  M1 = 100,
  M2 = 5,
  M3 = 1,
  p2s1 = 1,
  emp = 0,
  bw = "mserd",
  ker = "triangular",
  bwidth = 1,
  p1 = 1,
  conf = 95,
  upper = 1,
  covs1 = NULL,
  up = NULL,
  lo = NULL
)
}
\arguments{
\item{y}{A numeric vector of the outcome variable.}

\item{x}{A numeric vector of the assignment variable (forcing variable).}

\item{cut}{A numeric value indicating the cutoff point.}

\item{seed}{A seed number for reproducibility. Default is 1.}

\item{M1}{Number of imputations for MIRDD. Default is 100.}

\item{M2}{Number of imputations for visualization. Default is 5.}

\item{M3}{Number of imputations for specific plots. Default is 1.}

\item{p2s1}{Integer for Amelia's p2s argument (0, 1, or 2). Default is 1.}

\item{emp}{Amelia's empriical prior. Default is 0.}

\item{bw}{Bandwidth selection method for rdrobust. Default is "mserd".}

\item{ker}{Kernel function for rdrobust. Default is "triangular".}

\item{bwidth}{Scaling factor for bandwidth. Default is 1.}

\item{p1}{Polynomial order for rdrobust. Default is 1.}

\item{conf}{Confidence level (0-100). Default is 95.}

\item{upper}{If 1 (default), treatment is x >= cut. If 0, treatment is x < cut.}

\item{covs1}{Optional covariates for Amelia.}

\item{up}{Optional upper bound for plots.}

\item{lo}{Optional lower bound for plots.}
}
\value{
No return value, called for side effects (plotting and printing results).
}
\description{
Estimates local average treatment effects based on regression discontinuity
designs (RDD) and multiple imputation regression discontinuity designs (MIRDD).
It provides diagnostic tools for RDD by comparing results with those from MIRDD.
}
\examples{
# Example usage with dummy data
x <- runif(100, -1, 1)
y <- 0.5 * x + (x >= 0) + rnorm(100, 0, 0.1)
MIdiagRDD(y = y, x = x, cut = 0)
}
